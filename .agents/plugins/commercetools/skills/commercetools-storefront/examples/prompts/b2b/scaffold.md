This is an automated CI run. Do not ask any questions, request clarification, or wait for user input at any point. Make reasonable assumptions and proceed autonomously to completion.

Generate a complete production ready B2B storefront connected to commercetools.

## Output location

Write the storefront **at the repository root** (the current working directory). The Next.js app
root — the directory containing `package.json` — must be the repository root itself. Do **not** wrap
the generated project in an `output/` (or any other) subdirectory. You may create the usual app
subdirectories the skill prescribes (e.g. `app/`, `lib/`, `components/`).

## Required features

1. Business unit dashbaord
2. product-listing
3. product-detail
4. cart
5. checkout
6. customer authentication
7. Approval rules and workflows
8. Purchase lists
9. Quotes

Complete each phase fully before starting the next.

## Scope

**Skip ALL Optional Features.** Do not implement: superuser or wishlists.
